name: Build Custom Windows Client

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Custom Client Exe
    runs-on: windows-2022

    env:
      VCPKG_ROOT: C:\vcpkg
      LIBCLANG_PATH: C:\Program Files\LLVM\bin

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Rust Compiler First
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc

      - name: Force Modern Rust Binary Priority
        run: |
          echo "$USERPROFILE\.cargo\bin" >> $GITHUB_PATH
          echo "C:\vcpkg" >> $GITHUB_PATH
          echo "C:\Program Files\LLVM\bin" >> $GITHUB_PATH
        shell: powershell

      - name: Install LLVM & System Tools
        run: |
          choco install llvm wget unzip -y
        shell: powershell

      - name: Compile Static vcpkg Dependencies
        run: |
          vcpkg install libvpx:x64-windows-static libyuv:x64-windows-static opus:x64-windows-static aom:x64-windows-static --classic
        shell: powershell

      - name: Setup Flutter Toolchain
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.x'
          cache: true

      - name: Patch Cargo Manifest via Python
        run: |
          import os
          with open("Cargo.toml", "r", encoding="utf-8") as f:
              lines = f.readlines()
          new_lines = []
          for line in lines:
              if 'default-run = "rustdesk"' in line or 'default-run = "RustDesk"' in line:
                  continue
              if "tray-icon" in line and "git" in line:
                  line = 'tray-icon = "0.21.3"\n'
              new_lines.append(line)
          with open("Cargo.toml", "w", encoding="utf-8") as f:
              f.writelines(new_lines)
          print("Cargo.toml successfully patched line-by-line!")
        shell: python

      - name: Hardcode Server Details into Source Code
        run: |
          import os
          
          # Target the core config file where RustDesk fallback strings are declared
          config_path = os.path.join("libs", "hbb_common", "src", "config.rs")
          
          if not os.path.exists(config_path):
              raise FileNotFoundError(f"Could not locate config file at: {config_path}")
              
          with open(config_path, "r", encoding="utf-8") as f:
              content = f.read()
          
          # Inject custom values by modifying fallback functions or variable definitions
          content = content.replace('pub const RENDEZVOUS_SERVER: &str = "";', 'pub const RENDEZVOUS_SERVER: &str = "23.94.112.131:21116";')
          content = content.replace('pub const API_SERVER: &str = "";', 'pub const API_SERVER: &str = "http://23.94.112.131:21121";')
          content = content.replace('pub const RS_PUB_KEY: &str = "";', 'pub const RS_PUB_KEY: &str = "YqvrjbVRZ0uU6gQanlTEw6EM+KObuIfUuNa/5aXRCPs=";')
          
          # Double check alternative empty declaration formats used in newer branches
          content = content.replace('const RENDEZVOUS_SERVER: &str = "";', 'const RENDEZVOUS_SERVER: &str = "23.94.112.131:21116";')
          content = content.replace('const API_SERVER: &str = "";', 'const API_SERVER: &str = "http://23.94.112.131:21121";')
          content = content.replace('const RS_PUB_KEY: &str = "";', 'const RS_PUB_KEY: &str = "YqvrjbVRZ0uU6gQanlTEw6EM+KObuIfUuNa/5aXRCPs=";')

          with open(config_path, "w", encoding="utf-8") as f:
              f.write(content)
          print("Server details successfully hardcoded into config.rs source code!")
        shell: python

      - name: Synchronize Lockfile To Custom Manifest
        run: |
          cargo update
        shell: powershell

      - name: Bootstrap Rust Bridge Codegen
        run: |
          $env:LIBCLANG_PATH = "C:\Program Files\LLVM\bin"
          $env:PATH = "C:\Program Files\LLVM\bin;" + $env:PATH
          
          cargo install flutter_rust_bridge_codegen --version 1.80.1 --features uuid
          
          python -c "
          with open('flutter/pubspec.yaml', 'r', encoding='utf-8') as f:
              lines = f.readlines()
          
          out = []
          overrides_found = False
          for line in lines:
              out.append(line)
              if line.strip().startswith('dependency_overrides:'):
                  out.append('  extended_text: 13.0.0\n')
                  overrides_found = True
          
          if not overrides_found:
              out.append('\ndependency_overrides:\n  extended_text: 13.0.0\n')
              
          with open('flutter/pubspec.yaml', 'w', encoding='utf-8') as f:
              f.writelines(out)
          print('pubspec.yaml successfully injected with extended_text override!')
          "
          
          cd flutter
          flutter pub get
          cd ..
          
          flutter_rust_bridge_codegen --rust-input src/flutter_ffi.rs --dart-output flutter/lib/generated_bridge.dart
        shell: powershell

      - name: Build RustDesk Windows Client
        env:
          # CRITICAL: Bound directly to this execution step for option_env! macros
          RENDEZVOUS_SERVER: "23.94.112.131:21116"
          API_SERVER: "http://23.94.112.131:21121"
          RS_PUB_KEY: "YqvrjbVRZ0uU6gQanlTEw6EM+KObuIfUuNa/5aXRCPs="
        run: |
          $env:RUSTFLAGS="-A warnings"
          python build.py --flutter
        shell: powershell

      - name: Packaging Native Installer File
        run: |
          cd flutter/build/windows/x64/runner/Release/
          cp rustdesk.exe rustdesk-install.exe
        shell: powershell

      - name: Upload Finished Executable & Installer
        uses: actions/upload-artifact@v4
        with:
          name: RustDesk-Windows-Client-Package
          path: flutter/build/windows/x64/runner/Release/
